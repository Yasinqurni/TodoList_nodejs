const UserRepository = require('./users-repository')
const TitleRepository = require('./titles-repository')
const ActivityRepository = require('./activities-repository')


module.exports = {
    UserRepository,
    TitleRepository,
    ActivityRepository,
}