const AuthController = require('./auth-controller')
const TitleController = require('./titles-controller')
const ActivityController = require('./activities-controller')

module.exports = {
    AuthController,
    TitleController,
    ActivityController,
}